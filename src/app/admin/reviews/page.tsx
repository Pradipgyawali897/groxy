import { EmptyPanel } from "@/features/dashboard/empty-panel";

export default function AdminReviewsPage() {
  return (
    <EmptyPanel
      title="Review moderation lives here"
      description="The schema and route are ready for moderation queues, featured reviews, and quality-control workflows."
    />
  );
}
