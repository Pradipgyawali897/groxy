import { EmptyPanel } from "@/features/dashboard/empty-panel";

export default function CustomerOrdersPage() {
  return (
    <EmptyPanel
      title="Orders and tracking are structured here"
      description="Order history, fulfilment states, and future shipment updates will live on this page once order data is flowing."
    />
  );
}
